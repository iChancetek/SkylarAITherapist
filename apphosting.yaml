# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  maxInstances: 1
# Secrets are now managed at runtime via Google Cloud Secret Manager SDK
# to bypass build-time IAM permission issues.
# See src/lib/secrets.ts
